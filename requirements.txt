# Core dependencies for JSON parsing, file ops, and optional XML parsing
xmltodict==0.13.0

# If your formatter already uses any data processing helpers
python-dateutil==2.9.0.post0

# If your formatter already uses requests to fetch URLs
requests==2.32.3

# Command line interface
click==8.1.7

# If you want to pretty-print or validate JSON
orjson==3.10.7

# Optional: rich terminal output for debugging
rich==13.9.1

# PDF text extraction libraries (in order of preference)
pdfplumber==0.10.3
PyPDF2==3.0.1
PyMuPDF==1.23.26

# HTML parsing for bill text extraction
beautifulsoup4==4.12.3
